#include "mbed.h"



AnalogIn input(A2);

DigitalOut Selection1(D3);
DigitalOut Selection2(D4);
DigitalOut Selection3(D5);
DigitalOut Selection4(D6);

canBus

// main() runs in its own thread in the OS
int main()
{
    while (true) {

        ThisThread::sleep_for(1ms)

    }
}

